export class User{
    firstName: string; 
    lastName: string;
    role: string; 
    id: number;
    email: string; 
    password: string;

    constructor(id: number, firstName: string, lastName: string, role: string, email: string, password: string){
        
    }
}